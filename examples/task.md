# Example task

Use a disposable sample project. Update its greeting text from Hello to Welcome.
Keep the change within the specified file and do not install dependencies or contact external services.

Acceptance: running the sample project displays Welcome; existing checks pass.
Return the changed path and the check result for Codex's independent review.
